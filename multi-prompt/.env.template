# Azure OpenAI Configuration
AZURE_API_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_API_KEY=your-api-key-here
MODEL_DEPLOYMENT_NAME=gpt-4-turbo
API_VERSION=2024-02-15-preview

# GitHub Configuration
GITHUB_TOKEN=ghp_your-token-here

# Logging
LOG_LEVEL=info

# Server Configuration
HOST=0.0.0.0
PORT=8000

# Retry Configuration
MAX_RETRIES=3
RETRY_DELAY_MS=1000

# Optional: OpenTelemetry tracing
OTEL_ENABLED=false
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4317

# AWS Bedrock (used automatically when MODEL_DEPLOYMENT_NAME starts with "claude")
# When using Bedrock, AZURE_API_ENDPOINT and AZURE_API_KEY can be left unset.
# Authentication relies on the AWS SDK default credential chain
# (IAM role, instance profile, ECS task role, etc.).
# MODEL_DEPLOYMENT_NAME=claude-3-5-sonnet-20241022
AWS_REGION=us-east-1
